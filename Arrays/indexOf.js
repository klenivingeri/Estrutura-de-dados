const text = 'Devolve o index do primeiro elemento encontrado, se não encontrar devolve -1'
  const arr = ['um', 'dois', 'três', 'quatro', 'cinco']

const fIndexOf = (x) => {
  console.log(text)
  //return  arr.indexOf(x)
  return arr.indexOf(x)
}
module.exports = { fIndexOf }